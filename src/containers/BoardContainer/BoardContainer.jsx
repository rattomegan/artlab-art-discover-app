import Board from "../../components/Board/Board"

function BoardContainer({ allResults }) {
  return (
    <div>
      BoardContainer
      <Board allResults={allResults} />
    </div>
  )
}

export default BoardContainer;